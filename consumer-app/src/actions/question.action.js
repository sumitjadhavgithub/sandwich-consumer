import axios from "axios";

export const questionsGet = (payload) => {
  return async (dispatch) => {
     try {
      try {
         
        return response;
      } catch (e) {
        console.log(e);
      }
    } catch (error) {
      
    }
  };
};
